<UserControl x:Class="APartners.Views.ShopsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:APartners.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="0.5*"/>
            <ColumnDefinition />
        </Grid.ColumnDefinitions>

        <ListBox ItemsSource="{Binding Shops}" SelectedItem="{Binding SelectedShop}">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Margin="8">
                        <!-- Имя большими -->
                        <TextBlock
                           FontSize="18" 
                           FontWeight="Bold" 
                           TextWrapping="Wrap"
                           TextAlignment="Left"
                           Text="{Binding Name}" />

                        <!-- Описание маленькими -->
                        <TextBlock Text="{Binding Description}" 
                           FontSize="12" 
                           Foreground="Gray"
                           TextWrapping="Wrap" />
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <StackPanel Grid.Column="1">
            <Label Content="Название:" />
            <Label Content="{Binding SelectedShop.Name}" />
            <Label Content="Описание:" />
            <Label Content="{Binding SelectedShop.Description}" />
        </StackPanel>

    </Grid>
</UserControl>
