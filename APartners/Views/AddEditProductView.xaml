<UserControl x:Class="APartners.Views.AddEditProductView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:APartners.Views"
             xmlns:local2="clr-namespace:APartners.Converters"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="900">

    <UserControl.Resources>
        <local2:ImageEqualityConverter x:Key="ImageEqualityConverter"/>
        <local2:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
    </UserControl.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="2*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 📷 Фотографии -->
        <Border Grid.Row="0" CornerRadius="10" BorderBrush="#DDD" BorderThickness="1" Background="White" Margin="0 0 0 10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="350"/>
                    <ColumnDefinition />
                </Grid.ColumnDefinitions>
                
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="250"/>
                        <ColumnDefinition Width="100"/>
                    </Grid.ColumnDefinitions>

                    <!-- Главное фото -->
                    <Border BorderBrush="#DDD" BorderThickness="1"  Margin="10 10 0 10">
                        <Image Source="{Binding SelectedImage}" Stretch="Uniform" />
                    </Border>

                    <Grid Grid.Column="1" >
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="30"/>
                        </Grid.RowDefinitions>

                        <!-- Галерея -->
                        <Border BorderBrush="#DDD" BorderThickness="1"  Margin="0 10 0 0">
                            <ScrollViewer VerticalScrollBarVisibility="Hidden" HorizontalScrollBarVisibility="Auto">
                                <ItemsControl ItemsSource="{Binding ProductImages}">
                                    <ItemsControl.ItemsPanel>
                                        <ItemsPanelTemplate>
                                            <StackPanel Orientation="Vertical"/>
                                        </ItemsPanelTemplate>
                                    </ItemsControl.ItemsPanel>
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Border BorderThickness="2" Margin="5">
                                                <Border.BorderBrush>
                                                    <MultiBinding Converter="{StaticResource ImageEqualityConverter}">
                                                        <Binding />
                                                        <Binding Path="SelectedImage" />
                                                    </MultiBinding>
                                                </Border.BorderBrush>
                                                <Image Source="{Binding}" Width="80" Height="80" Stretch="Uniform" Cursor="Hand"
                                           MouseLeftButtonDown="Image_MouseLeftButtonDown"/>
                                            </Border>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </ScrollViewer>
                        </Border>

                        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center">
                            <Button Command="{Binding UpdateImageCommand}" Margin="5">
                                <Image Source="https://images.icon-icons.com/3858/PNG/64/add_file_insert_file_new_file_icon_238367.png" />
                            </Button>
                            <Button Command="{Binding DeleteImageCommand}" Margin="5">
                                <Image Source="https://images.icon-icons.com/3858/PNG/64/close_file_remove_file_icon_238366.png" />
                            </Button>
                        </StackPanel>
                    </Grid>

                </Grid>

                <Grid Grid.Column="1">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition />
                        <ColumnDefinition Width="2*"/>
                    </Grid.ColumnDefinitions>
                    <!-- 💰 Параметры товара -->
                    <StackPanel Margin="10 0 10 10">
                        <TextBlock Text="Цена (Руб.)" Margin="0, 10, 0, 0"/>
                        <TextBox Style="{StaticResource OneLine}"  Margin="0, 10, 0, 0"/>

                        <TextBlock Text="Количество" Margin="0, 10, 0, 0"/>
                        <TextBox Style="{StaticResource OneLine}" Margin="0, 10, 0, 0"/>

                        <TextBlock Text="Категория" Margin="0, 10, 0, 0"/>
                        <ComboBox Style="{StaticResource OneLineComboBox}"  Margin="0, 10, 0, 0"/>
                    </StackPanel>

                    <Border Grid.Column="1" BorderBrush="#DDD" BorderThickness="1, 0, 0, 0">
                        <StackPanel >
                            <TextBlock Text="Магазины" Margin="10, 0"/>
                            <DataGrid Grid.ColumnSpan="2" Grid.Row="1" Margin="10, 0" AutoGenerateColumns="False" ItemsSource="{Binding Shops}" CanUserAddRows="False">
                                <DataGrid.Columns>
                                    <DataGridCheckBoxColumn Width="30"
                                                            Header=""
                                                            Binding="{Binding IsChecked, Mode=TwoWay}" IsReadOnly="False" />
                                    <DataGridTextColumn Width="0.3*" Header="Название" Binding="{Binding Name}" IsReadOnly="True" />
                                    <DataGridTextColumn Width="0.5*" Header="Адрес" Binding="{Binding Address}" IsReadOnly="True" />
                                </DataGrid.Columns>
                            </DataGrid>
                        </StackPanel>
                    </Border>
                </Grid>
            </Grid>
        </Border>

        <!-- 📝 Основная информация -->
        <Border Grid.Row="1" Background="White" CornerRadius="10" Padding="10" Margin="0 0 0 10" BorderBrush="#DDD" BorderThickness="1">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="150"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <TextBlock Text="Название:" VerticalAlignment="Center" Margin="5"/>
                <TextBox Grid.Column="1" Style="{StaticResource OneLine}" Text="{Binding Product.Name}" Margin="5"/>

                <TextBlock Text="Описание:" Grid.Row="1" VerticalAlignment="Top" Margin="5"/>
                <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding Product.Description}" Margin="5" 
                         TextWrapping="Wrap" AcceptsReturn="True" Height="80"/>
            </Grid>
        </Border>


        <!-- ✅ Кнопка сохранить -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Сохранить" Command="{Binding SaveCommand}" Padding="10,5" Margin="5"/>
        </StackPanel>
    </Grid>
</UserControl>
